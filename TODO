- make it possible to download files through the mgmt interface
- write a few plugins
- handle container formats of other downloaders (mainly ccf and rsdf. won't support DLC because it's not open-source friendly)
- improve speed regulation
- Make a multiuser-system out of downloaddaemon with partly individual configuration (download-folder) and administration...
- remotely solve captchas if they can't be solved automatically
- auto-updater for plugins and reconnect scripts
- plugin-properties: makes it possible to set plugin-specific options if hte plugin can handle them (e.g. one-time passwords for containers, etc)
- execute custom command after finishing a download
- "insecure-mode": all variables can be changed remotely (for private networks behind firewall)
- use Angelscript or similar for plugins and implement auto-update functionality for plugins
